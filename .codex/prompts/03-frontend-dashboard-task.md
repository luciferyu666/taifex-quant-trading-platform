# Frontend Dashboard Task

Work in `frontend/`. Build simple, responsive App Router UI with plain CSS. The dashboard must render safely when backend services are unavailable and must never expose broker credentials or direct broker controls.
