# Phase 3 Strategy SDK

Read `AGENTS.md` and `docs/implementation-roadmap.md` first. Inspect the repository before editing.

Implement only Phase 3 or the smallest safe strategy SDK slice:
- signal-only strategy contracts
- base strategy interface
- example paper-safe strategy
- strategy registry tests or docs

Do not add order submission APIs. Do not import broker SDKs in `strategy-engine/`. Do not enable live trading.

Run `make check` before finishing. Report files changed, checks passed, blockers, and the exact next command.
