# Backend API Task

Work in `backend/`. Keep FastAPI routes small, typed, and config-driven. Preserve `TRADING_MODE=paper` defaults, add tests for every endpoint, and ensure no route can bypass Risk Engine or OMS concepts.
