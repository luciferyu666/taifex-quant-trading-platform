# Start Here

Read `AGENTS.md`, `docs/web-app-ai-pipeline.md`, and this prompt. Inspect the repository, run the narrow relevant checks first, then propose or implement the next smallest safe vertical slice. Preserve paper-trading defaults and never add secrets. Use `.codex/prompts/20-web-app-ai-pipeline.md` for repeatable Web App tasks.
