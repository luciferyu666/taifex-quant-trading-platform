# Risk and OMS Task

Preserve the rule: future orders flow through Risk Engine and OMS before Broker Gateway. Implement paper-only, idempotent, testable state transitions with conservative defaults. Never let strategies call broker adapters directly.
