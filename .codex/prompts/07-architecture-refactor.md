# Architecture Refactor

Refactor only when it clarifies service boundaries or removes real duplication. Keep changes incremental, document the boundary in `docs/`, and run checks before finishing. Do not introduce live trading behavior.
