# Phase 5 Command Center

Read `AGENTS.md` and `docs/implementation-roadmap.md` first. Inspect the repository before editing.

Implement only Phase 5 or the smallest safe Web Command Center slice:
- roadmap phase cards
- safety mode card
- contract table
- risk status card
- paper-only order simulation placeholder
- backend-unavailable fallback UI

Do not add live order buttons. Do not collect broker credentials. Do not enable live trading. Label all order-related UI as Paper Only.

Run `make check` before finishing. Report files changed, checks passed, blockers, and the exact next command.
