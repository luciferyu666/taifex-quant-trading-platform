# Start Here

Read `AGENTS.md` and this prompt. Inspect the repository, run `make check`, then propose the next smallest safe vertical slice. Implement only after understanding the current state. Preserve paper-trading defaults and never add secrets.
