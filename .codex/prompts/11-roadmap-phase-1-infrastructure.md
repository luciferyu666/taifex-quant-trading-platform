# Phase 1 Infrastructure Foundation

Read `AGENTS.md` and `docs/implementation-roadmap.md` first. Inspect the repository before editing.

Implement only Phase 1 or the smallest safe infrastructure slice:
- Docker Compose and local service checks
- Kubernetes placeholder manifests
- Vault placeholder documentation
- observability placeholder documentation
- roadmap status script

Do not deploy Kubernetes. Do not store secrets. Do not enable live trading. Keep the paper broker as default.

Run `make check` before finishing. Report files changed, checks passed, blockers, and the exact next command.
